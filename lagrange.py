from pylab import*
x=array[2,2.5,3]
y=array[0.69315,0.91629,1.09861]
n=len(x)
def lang(x_val):
    y_val=0
    for i in range(n):
        for j in range(n):
            if i!=j:
                y_val=y_val+((x_val-x[i])*(x_val-)/()())